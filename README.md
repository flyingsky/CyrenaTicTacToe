CyrenaTicTacToe
===============

1. Install node
2. check out code with git or just download code
3. cd to project directory and run command `npm install`
4. start you server from shell `node app.js`
5. open two browsers and access "localhost:3000"
6. select users and start to play, you can see the same result from both browsers
